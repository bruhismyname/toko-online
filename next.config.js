/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["www.converse.id", "www.converse.co.th", "i.ytimg.com"],
  },
};

module.exports = nextConfig;
